name: ht_categories_firestore
description: A Firestore implementation of the HtCategoriesClient interface, providing CRUD operations for categories using Cloud Firestore.
repository: https://github.com/headlines-toolkit/ht-categories-firestore
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ^3.24.0

dependencies:
  cloud_firestore: ^5.6.6
  flutter:
    sdk: flutter
  ht_categories_client:
    git:
      url: https://github.com/headlines-toolkit/ht-categories-client.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0
